//This is a starter kit for p5 projects

function setup(){
  createCanvas(windowWidth,windowHeight);
}

function draw(){
  background("cadetblue");

  ellipse(windowWidth/2,windowHeight/2,100,100
    
}
